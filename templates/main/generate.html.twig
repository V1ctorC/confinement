{% extends 'base.html.twig' %}

{% block title %}Génération du PDF{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; border: 2px solid; text-align: center}
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Attestion de déplacement dérogatoire</h1>

        <p>
            En application de l’article 3 du décret du 23 mars 2020 prescrivant les mesures générales <br />
            nécessaires pour faire face à l’épidémie de Covid19 dans le cadre de l’état d’urgence sanitaire
        </p>

        <div>
            <span>Je soussigné(e),</span> <br />

            <div style="text-align: initial">
                <span>Mme/M. :</span>
                <span>{{ data['lastname'] }} {{ data['firstname'] }}</span>
            </div>
            <div style="text-align: initial">
                <span>Né(e)le :</span>
                <span>{{ data['birthdate']|date("d/m/Y") }}</span>
            </div>
            <div style="text-align: initial">
                <span>À :</span>
                <span>{{ data['birthplace'] }}</span>
            </div>
            <div style="text-align: initial">
                <span>Demeurant :</span>
                <div>
                    {{ data['address'] }} <br />
                    {{ data['zipcode'] }}, {{ data['city'] }}
                </div>
            </div>
        </div>

        <p>
            Certifie que mon déplacement est lié au motif suivant autorisé par l’article 3 du
            décret du 23 mars 2020 prescrivant les mesures générales nécessaires pour faire face à
            l’épidémie de Covid19 dans le cadre de l’état d’urgence sanitaire :
        </p>


        <p>
            Vous allez pouvoir télécharger votre PDF très prochainement
        </p>

        {{ dump(data) }}

        {{ data['firstname'] }}
        {{ data['lastname'] }}
        {{ data['birthdate']|date("d/m/Y") }}
        {{ data['address'] }}
        {{ data['zipcode'] }}
        {{ data['city'] }}

        {{ data['approveCity'] }}

    </div>
{% endblock %}
